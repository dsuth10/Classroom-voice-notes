# Implementation Plan: [FEATURE]

**Branch**: `[###-feature-name]` | **Date**: [DATE] | **Spec**: [link]

**Input**: Feature specification from `/specs/features/[###-feature-name]/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `specs/templates/plan-template.md` for the execution workflow.

## Summary

[Extract from feature spec: primary requirement + technical approach from research]

## Technical Context

<!--
  ACTION REQUIRED: Replace the content in this section with the technical details
  for the project. The structure here is presented in advisory capacity to guide
  the iteration process.
-->

**Language/Version**: [e.g., Python 3.11, PySide6 or NEEDS CLARIFICATION]

**Primary Dependencies**: [e.g., whisper.cpp, porcupine, ollama or NEEDS CLARIFICATION]

**Storage**: [if applicable, e.g., Obsidian Markdown files, local sqlite, or N/A]

**Testing**: [e.g., pytest, unittest or NEEDS CLARIFICATION]

**Target Platform**: [e.g., Windows 10/11 desktop, local microphone or NEEDS CLARIFICATION]

**Project Type**: [e.g., desktop-app/web-service/cli or NEEDS CLARIFICATION]

**Performance Goals**: [domain-specific, e.g., transcription < 5s, wake-word detection latency < 100ms or NEEDS CLARIFICATION]

**Constraints**: [domain-specific, e.g., offline-capable, strict local storage, no cloud API calls or NEEDS CLARIFICATION]

**Scale/Scope**: [domain-specific, e.g., 1 user (teacher), local processing only or NEEDS CLARIFICATION]

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

[Gates determined based on constitution file at specs/constitution.md]

## Project Structure

### Documentation (this feature)

```text
specs/features/[###-feature]/
├── plan.md              # This file
├── research.md          # Technical research notes
├── data-model.md        # Data model schema definitions
├── quickstart.md        # Quickstart setup instructions
├── contracts/           # API/schema contract details
└── tasks.md             # Task breakdown generated by /speckit.tasks
```

### Source Code (repository root)

```text
app/
├── main.py
├── config/
├── audio/
├── wakeword/
├── transcription/
├── ollama_router/
├── destinations/
├── audit/
├── ui/
└── utils/

tests/
├── integration/
└── unit/
```

**Structure Decision**: [Document the selected structure and reference the real directories captured above]

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| [e.g., Cloud fallback] | [current need] | [why local model is insufficient] |
